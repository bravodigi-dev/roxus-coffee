const cart=[];const drawer=document.getElementById("cartDrawer");const backdrop=document.getElementById("backdrop");const items=document.getElementById("cartItems");const count=document.getElementById("cartCount");const total=document.getElementById("cartTotal");const toast=document.getElementById("toast");function money(n){return`RM ${n.toFixed(2)}`}function message(t){toast.textContent=t;toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),1800)}function render(){count.textContent=cart.length;total.textContent=money(cart.reduce((s,i)=>s+i.price,0));if(!cart.length){items.innerHTML='<p class="empty">Your cart is empty.</p>';return}items.innerHTML=cart.map((i,x)=>`<div class="cart-item"><div><p>${i.name}</p><small>${money(i.price)}</small></div><button data-remove="${x}">Remove</button></div>`).join("");document.querySelectorAll("[data-remove]").forEach(b=>b.onclick=()=>{cart.splice(Number(b.dataset.remove),1);render()})}document.querySelectorAll(".add").forEach(b=>b.onclick=()=>{cart.push({name:b.dataset.name,price:Number(b.dataset.price)});render();message(b.dataset.name+" added to cart")});document.getElementById("cartButton").onclick=()=>{drawer.classList.add("open");backdrop.classList.add("show");document.body.classList.add("locked")};function close(){drawer.classList.remove("open");backdrop.classList.remove("show");document.body.classList.remove("locked")}document.getElementById("closeCart").onclick=close;backdrop.onclick=close;document.getElementById("checkout").onclick=()=>message(cart.length?"Checkout demo ready for payment integration":"Your cart is empty");document.querySelectorAll(".filter").forEach(b=>b.onclick=()=>{document.querySelectorAll(".filter").forEach(x=>x.classList.remove("active"));b.classList.add("active");document.querySelectorAll("#coffeeGrid .product-card").forEach(c=>c.classList.toggle("hidden",b.dataset.filter!=="all"&&c.dataset.category!==b.dataset.filter))});document.querySelectorAll("[data-plan]").forEach(b=>b.onclick=()=>message(b.dataset.plan+" selected"));const nav=document.getElementById("navMenu");document.getElementById("mobileMenu").onclick=()=>nav.classList.toggle("open");nav.querySelectorAll("a").forEach(a=>a.onclick=()=>nav.classList.remove("open"));const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add("visible")}),{threshold:.12});document.querySelectorAll(".reveal").forEach(e=>observer.observe(e));